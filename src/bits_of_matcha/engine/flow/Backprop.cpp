#include "bits_of_matcha/engine/flow/Backprop.h"


namespace matcha::engine {

void Backprop::makeCache(const std::unique_ptr<Graph>& graph) {}
void Backprop::loadCache(std::unique_ptr<Graph>& graph) {}

}